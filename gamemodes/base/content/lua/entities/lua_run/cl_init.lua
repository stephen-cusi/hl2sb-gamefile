--========== HL2SB - GMod compat ===========--
--
-- Purpose: 客户端半边。lua_run 没有客户端行为，只需要加载共享表
--          （ENT.Type / DisableDuplicator / 访问器方法）。
--
--===========================================================================--

include( "shared.lua" )
